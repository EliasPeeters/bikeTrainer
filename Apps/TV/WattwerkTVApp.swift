import SwiftUI
import WattwerkUI

/// The Apple TV app. Same code, bigger type, driven by the remote.
@main
struct WattwerkTVApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
        }
    }
}
