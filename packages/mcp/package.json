{
  "name": "@wattwerk/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Wattwerk MCP-Server: die API als Werkzeuge fuer Sprachmodelle",
  "main": "src/index.ts",
  "bin": {
    "wattwerk-mcp": "dist/stdio.js"
  },
  "scripts": {
    "start": "ts-node ./src/stdio.ts",
    "build": "rm -rf dist && esbuild ./src/stdio.ts ./src/serve.ts --bundle --platform=node --target=node20 --format=cjs --outdir=dist --packages=external --alias:@wattwerk/shared=../shared/src/index.ts --banner:js=\"#!/usr/bin/env node\" && chmod +x dist/stdio.js dist/serve.js",
    "start:dist": "node dist/stdio.js",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "jest",
    "serve": "ts-node ./src/serve.ts",
    "serve:dist": "node dist/serve.js",
    "build:bundle": "node ./scripts/build-bundle.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@wattwerk/shared": "*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.2",
    "esbuild": "^0.24.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
